DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=shopmate_ai
DB_USER=root
DB_PASSWORD=
AI_SERVICE_PORT=8001

# Optional: OpenAI-compatible chat completion backend (local Ollama's /v1
# endpoint, or a hosted gateway). Leave LLM_API_BASE empty to keep using
# the rule-based nlp.py pipeline.
LLM_API_BASE=
LLM_API_KEY=
LLM_MODEL=
