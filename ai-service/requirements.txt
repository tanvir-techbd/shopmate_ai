fastapi>=0.141
uvicorn[standard]>=0.52
sqlalchemy>=2.0
pymysql>=1.2
scikit-learn>=1.9
python-dotenv>=1.2
pydantic>=2.13
httpx>=0.27

# scipy arrives transitively via scikit-learn, but is pinned below 1.18
# because Windows Smart App Control blocks freshly-released, low-reputation
# native DLLs: scipy 1.18.1's _batched_linalg fails to load with "An
# Application Control policy has blocked this file", which takes scikit-learn
# (and so the whole AI service) down with it. 1.15.3 is old enough to be
# trusted by that policy and satisfies scikit-learn's own scipy>=1.10.
# Harmless on Linux - drop this pin if the service ever moves off Windows.
scipy<1.18
